export enum CardType {
  DEBIT = 'debit',
  CREDIT = 'credit',
}

export enum CardStatus {
  ACTIVE = 'active',
  LOCKED = 'locked',
}

export enum Provider {
  VISA = 'visa',
  MASTERCARD = 'mastercard',
}
